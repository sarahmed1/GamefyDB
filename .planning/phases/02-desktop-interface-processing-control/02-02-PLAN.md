---
phase: 02-desktop-interface-processing-control
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/worker.py
  - src/pipeline/orchestrator.py
autonomous: true
requirements:
  - PIPE-01
must_haves:
  truths:
    - "A background worker can execute the data pipeline orchestrator"
    - "The worker emits signals with progress and log updates"
    - "The pipeline can be instructed to stop gracefully between batches"
  artifacts:
    - path: "src/ui/worker.py"
      provides: "QThread logic for running pipeline asynchronously"
      contains: "QThread"
  key_links:
    - from: "src/ui/worker.py"
      to: "src/pipeline/orchestrator.py"
      via: "run_pipeline execution"
---

<objective>
Implement a QThread-based background worker and adapt the orchestrator to emit progress and logs for the UI.

Purpose: Enables non-blocking UI during data extraction (PIPE-01) and sets up the foundation for graceful cancellation (D-04).
Output: A reusable QThread class for the pipeline and a modified orchestrator capable of callback/signal execution.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-desktop-interface-processing-control/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adapt Orchestrator for UI Signals (Callbacks)</name>
  <files>src/pipeline/orchestrator.py</files>
  <action>
    Modify `run_pipeline` to accept optional `progress_callback(int, int)` (current, total) and `log_callback(str)` arguments.
    Emit progress tracking overall file count.
    Add a `should_cancel: Callable[[], bool]` argument to `run_pipeline`. Before each batch or file, if `should_cancel()` returns `True`, log a graceful shutdown message, commit the current batch, and break early (D-04).
  </action>
  <verify>
    <automated>python -c "import ast; code = open('src/pipeline/orchestrator.py').read(); assert 'progress_callback' in code"</automated>
  </verify>
  <done>Orchestrator can report progress and stop on cancel flags gracefully.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Pipeline Worker Thread</name>
  <files>src/ui/worker.py</files>
  <action>
    Create a `PipelineWorker` subclass of `QThread`.
    Define custom PySide6 `Signal`s for `progress(int, int)`, `log(str)`, `finished()`, and `error(str)`.
    Define an `__init__` receiving the target directory path.
    Implement `run()` to call `run_pipeline`, wrapping it in try/except to emit the `error(str)` signal on failure.
    Pass lambdas mapping the orchestrator callbacks to emit the worker's signals.
    Implement a thread-safe `cancel()` method using an internal boolean flag (e.g., `_is_cancelled`). Pass a lambda checking this flag to `should_cancel`.
  </action>
  <verify>
    <automated>python -c "from PySide6.QtCore import QThread; import sys; from PySide6.QtWidgets import QApplication; app = QApplication(sys.argv); from src.ui.worker import PipelineWorker; w = PipelineWorker('dummy_path'); assert hasattr(w, 'run')"</automated>
  </verify>
  <done>PipelineWorker encapsulates QThread, maps callbacks to Signals, and exposes a cancel flag.</done>
</task>

</tasks>

<success_criteria>
The worker thread operates without blocking the main event loop and gracefully handles pipeline batch interruptions.
</success_criteria>

<output>
After completion, create `.planning/phases/02-desktop-interface-processing-control/02-02-SUMMARY.md`
</output>
