---
phase: 02-desktop-interface-processing-control
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - src/ui/main_window.py
autonomous: false
requirements:
  - UI-04
must_haves:
  truths:
    - "User can start the pipeline from the selected directory via a UI button"
    - "User can see real-time progress updates in the progress bar"
    - "User can view logs appended natively to the log viewer widget"
    - "User can cancel an active pipeline gracefully via a UI button"
    - "UI remains responsive while processing"
  artifacts:
    - path: "src/ui/main_window.py"
      provides: "Application logic bridging the GUI and Pipeline Worker"
      contains: "self.worker.start()"
  key_links:
    - from: "src/ui/main_window.py"
      to: "src/ui/worker.py"
      via: "worker signals (progress, log, finished) connected to UI slots"
---

<objective>
Wire the pipeline worker to the main PySide6 interface, enabling real-time feedback and safe cancellation while processing.

Purpose: Achieves UI responsiveness (UI-04) during heavy data extraction and completes the UX loop for Phase 2.
Output: A fully functional desktop application capable of running and cancelling the data pipeline.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-desktop-interface-processing-control/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Connect Start Logic and Worker State</name>
  <files>src/ui/main_window.py</files>
  <action>
    Import `PipelineWorker` into `main_window.py`.
    On clicking the Start button, validate a directory is selected. If not, append an error to the log.
    If valid, instantiate `PipelineWorker` with the selected path.
    Disable the Start button and directory selection. Enable the Cancel button.
    Connect worker signals:
    - `progress(int, int)` -> update the `QProgressBar` maximum to total, value to current.
    - `log(str)` -> append the text to the `QPlainTextEdit` log viewer.
    - `finished()` / `error(str)` -> re-enable Start/Directory buttons, disable Cancel button, log completion/error.
    Call `worker.start()`. Ensure `self.worker` is retained to prevent garbage collection.
  </action>
  <verify>
    <automated>python -c "import ast; code = open('src/ui/main_window.py').read(); assert 'PipelineWorker' in code"</automated>
  </verify>
  <done>Start button initiates the pipeline in the background and sets UI state correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Connect Cancellation Logic</name>
  <files>src/ui/main_window.py</files>
  <action>
    Wire the Cancel button to call `self.worker.cancel()`.
    Log "Cancellation requested. Finishing current batch gracefully..." immediately.
    Disable the Cancel button so it cannot be spammed. The `finished()` signal will reset the UI once the worker naturally exits its loop.
  </action>
  <verify>
    <automated>python -c "import ast; code = open('src/ui/main_window.py').read(); assert 'cancel()' in code"</automated>
  </verify>
  <done>Cancellation button correctly triggers the graceful shutdown mechanism.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify GUI functionality</name>
  <action>
    <what-built>A functional desktop PySide6 app capable of starting, logging, and gracefully stopping a local data processing pipeline.</what-built>
    <resume-signal>Type "approved" or describe issues observed during the run.</resume-signal>
  </action>
  <files>None</files>
  <verify>
    <how-to-verify>
      1. Run `python src/main.py`
      2. Click Browse, pick a directory (preferably with some dummy HTML files).
      3. Click Start.
      4. Observe the log pane populating and progress bar updating.
      5. Attempt to click Cancel mid-process.
      6. Ensure the UI remains responsive (doesn't hang) throughout the process.
      7. Ensure it says "Cancellation requested..." and cleanly finishes the current batch before stopping.
    </how-to-verify>
  </verify>
  <done>User confirmed everything is responsive and functional</done>
</task>

</tasks>

<success_criteria>
The complete end-to-end desktop experience functions seamlessly, with the pipeline running in a background thread and providing live feedback to the user without UI freezes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-desktop-interface-processing-control/02-03-SUMMARY.md`
</output>
