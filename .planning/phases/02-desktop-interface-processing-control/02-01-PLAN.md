---
phase: 02-desktop-interface-processing-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/main_window.py
  - src/main.py
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
must_haves:
  truths:
    - "User can launch a native PySide6 desktop window"
    - "User can click a button to open a standard directory selection dialog"
    - "User can see the selected directory path, a progress bar, and a log viewing area"
  artifacts:
    - path: "src/ui/main_window.py"
      provides: "Desktop interface layout and directory selection logic"
      contains: "QMainWindow"
  key_links:
    - from: "src/main.py"
      to: "src/ui/main_window.py"
      via: "QApplication execution"
---

<objective>
Build the core PySide6 desktop interface layout and implement standard directory selection functionality.

Purpose: Provides the visual shell for users to interact with the pipeline, fulfilling decisions D-01, D-02, and D-03.
Output: The main window component with directory selection wired up.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-desktop-interface-processing-control/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Main Window Layout</name>
  <files>src/ui/main_window.py</files>
  <action>
    Create a PySide6 QMainWindow subclass named `MainWindow`.
    Add a central widget with a vertical layout containing:
    1. A directory selection area (QLabel for path, QPushButton to browse).
    2. Controls area (Start and Cancel buttons).
    3. A QProgressBar for overall progress (D-02).
    4. A QPlainTextEdit set to read-only for logs (D-03).
    Ensure the layout is responsive and standard.
  </action>
  <verify>
    <automated>python -c "from PySide6.QtWidgets import QApplication; import sys; app = QApplication(sys.argv); from src.ui.main_window import MainWindow; w = MainWindow(); assert hasattr(w, 'show')"</automated>
  </verify>
  <done>MainWindow class exists with all required UI widgets instantiated and added to the layout.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Directory Selection (D-01)</name>
  <files>src/ui/main_window.py</files>
  <action>
    Wire the browse button to a slot that opens `QFileDialog.getExistingDirectory`.
    When a directory is selected, update the path label text to show the selected directory.
    If cancelled, keep the previous path.
  </action>
  <verify>
    <automated>python -c "import ast; code = open('src/ui/main_window.py').read(); assert 'QFileDialog' in code"</automated>
  </verify>
  <done>Directory selection dialog opens natively and updates the label.</done>
</task>

<task type="auto">
  <name>Task 3: Update Main Entry Point</name>
  <files>src/main.py</files>
  <action>
    Modify `src/main.py` to instantiate `QApplication` and show the `MainWindow` when run without CLI arguments, falling back to the UI instead of the previous CLI runner. Ensure `sys.exit(app.exec())` is called.
  </action>
  <verify>
    <automated>python -c "import ast; code = open('src/main.py').read(); assert 'QApplication' in code"</automated>
  </verify>
  <done>Running `python src/main.py` launches the PySide6 application.</done>
</task>

</tasks>

<success_criteria>
The PySide6 UI is defined, can be launched via `main.py`, and allows the user to select a directory using a native file dialog.
</success_criteria>

<output>
After completion, create `.planning/phases/02-desktop-interface-processing-control/02-01-SUMMARY.md`
</output>
