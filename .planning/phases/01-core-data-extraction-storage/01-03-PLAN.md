---
phase: 01-core-data-extraction-storage
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/pipeline/orchestrator.py
  - src/main.py
autonomous: false
requirements:
  - PIPE-01
  - PIPE-02
  - PIPE-03
  - PIPE-04

must_haves:
  truths:
    - "System can batch process a directory of HTML files"
    - "Data is successfully written to SQLite after normalization"
    - "User can manually run the pipeline end-to-end via CLI for testing"
  artifacts:
    - path: "src/pipeline/orchestrator.py"
      provides: "Batch orchestration and transaction logic"
      exports: ["run_pipeline"]
    - path: "src/main.py"
      provides: "CLI entry point"
  key_links:
    - from: "src/pipeline/orchestrator.py"
      to: "src/database/session.py"
      via: "session.add_all and session.commit"
      pattern: "commit"
---

<objective>
Wire the core pipeline modules together into a complete orchestrator that batch processes local `.html` files and persists the output to SQLite.

Purpose: End-to-end extraction, normalization, and insertion of data into the database.
Output: A working `run_pipeline` function and a simple CLI runner in `main.py`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-data-extraction-storage/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Batch Orchestrator</name>
  <files>src/pipeline/orchestrator.py, tests/test_orchestrator.py</files>
  <behavior>
    - Test 1: Given a mock directory, it orchestrates extraction, normalization, and db insertion in batches.
    - Test 2: Rolls back transactions if DB insertion fails entirely, or safely commits valid batch records.
  </behavior>
  <action>
    Implement `run_pipeline(target_directory, batch_size=50)` using `pathlib` to glob `.html` files.
    Per D-04: Process files in sequential batches.
    For each batch: 1. call `extract_html_file` on each file, 2. call `normalize_data` on the batch list, 3. get a DB session, convert schemas to SQLAlchemy models, `session.add_all()`, and `session.commit()`. Use `logging` to output batch progress.
  </action>
  <verify>
    <automated>pytest tests/test_orchestrator.py -v</automated>
  </verify>
  <done>Pipeline connects all modules correctly with batching.</done>
</task>

<task type="auto">
  <name>Task 2: CLI Wrapper</name>
  <files>src/main.py</files>
  <action>
    Create a basic CLI using `argparse` to allow running the pipeline for testing purposes before the PySide6 UI is built (Phase 2).
    Accept a `--target` directory argument. Call `run_pipeline(target)`. Initialize the database `init_db()` before running.
  </action>
  <verify>
    <automated>python src/main.py --help</automated>
  </verify>
  <done>CLI correctly parses arguments and can invoke the pipeline.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human Verification</name>
  <files>local.db</files>
  <action>Pause for human verification</action>
  <verify>Manual verification</verify>
  <done>User approved</done>
  <what-built>End-to-end data pipeline processing a local directory</what-built>
  <how-to-verify>
    1. Create a dummy directory with 2-3 `.html` files containing simple sample data.
    2. Run `python src/main.py --target ./dummy_dir`
    3. Check `local.db` using sqlite3 or DB Browser to ensure records were saved.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Ensure end-to-end testing processes HTML successfully into SQLite.
</verification>

<success_criteria>
- Batching works smoothly.
- Data correctly traverses Extractor -> Normalizer -> SQLite Database.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-data-extraction-storage/01-03-SUMMARY.md`
</output>
