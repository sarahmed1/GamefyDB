---
phase: 01-core-data-extraction-storage
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/pipeline/extractor.py
  - src/pipeline/normalizer.py
autonomous: true
requirements:
  - PIPE-02
  - PIPE-03

must_haves:
  truths:
    - "System can parse HTML and extract key fields using BeautifulSoup"
    - "Malformed HTML files are skipped instead of failing the pipeline"
    - "Data is normalized with pandas and validated with pydantic strictly"
    - "Invalid rows are dropped and logged, not saved"
  artifacts:
    - path: "src/pipeline/extractor.py"
      provides: "HTML Parsing Module"
      exports: ["extract_html_file"]
    - path: "src/pipeline/normalizer.py"
      provides: "Pandas-based normalization and Pydantic validation"
      exports: ["normalize_data"]
  key_links:
    - from: "src/pipeline/normalizer.py"
      to: "src/schemas/record.py"
      via: "pydantic model validation"
      pattern: "GameRecordSchema"
---

<objective>
Implement the core pipeline modules for HTML parsing and data normalization, ensuring malformed files are skipped and invalid rows are strictly dropped.

Purpose: Parse messy HTML files and convert the extracted contents into strictly validated datasets, preparing them for database storage.
Output: Extractor and Normalizer modules working in isolation.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-data-extraction-storage/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Key types from plan 01 needed here -->
From src/schemas/record.py:
```python
class GameRecordSchema(BaseModel):
    # fields defined in phase 01-01
    pass
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HTML Extractor</name>
  <files>src/pipeline/extractor.py, tests/test_extractor.py</files>
  <behavior>
    - Test 1: Given a valid sample HTML string, extract key fields (e.g. title, url, etc.) as a dict.
    - Test 2: Given a malformed/empty HTML file, skip and log warning.
  </behavior>
  <action>
    Implement `extract_html_file(filepath)` using `BeautifulSoup4` with the `lxml` parser.
    Per D-01: If HTML is unparseable or critical fields are missing, log a warning (using `logging`) and return `None` (skip the file) rather than failing.
    Return a flat dictionary of extracted fields to be passed to the normalizer.
  </action>
  <verify>
    <automated>pytest tests/test_extractor.py -v</automated>
  </verify>
  <done>Extractor handles both good and bad HTML correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Data Normalizer</name>
  <files>src/pipeline/normalizer.py, tests/test_normalizer.py</files>
  <behavior>
    - Test 1: Converts valid raw dicts into Pandas DataFrame, validates rows against Pydantic model.
    - Test 2: Drops invalid rows entirely and logs them as errors.
  </behavior>
  <action>
    Implement `normalize_data(raw_data_list)` using Pandas and Pydantic.
    Per D-02: Normalize strictness is set to strict. Load the dict list into a Pandas DataFrame for vectorized cleaning if needed (e.g., date parsing, string stripping).
    Iterate over rows or use `apply` to instantiate Pydantic schemas (from Plan 01). If a row fails validation, catch `ValidationError`, log it, and drop the row. Return a list of valid, normalized schema instances.
  </action>
  <verify>
    <automated>pytest tests/test_normalizer.py -v</automated>
  </verify>
  <done>Normalizer strictly drops invalid rows and returns only validated objects.</done>
</task>

</tasks>

<verification>
Unit tests prove both modules run, handle errors gracefully, and drop malformed data/HTML properly.
</verification>

<success_criteria>
- `extractor.py` skips bad HTML (D-01).
- `normalizer.py` strict drops bad rows (D-02).
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-data-extraction-storage/01-02-SUMMARY.md`
</output>
