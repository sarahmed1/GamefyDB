---
phase: 01-core-data-extraction-storage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/database/models.py
  - src/database/session.py
  - src/schemas/record.py
autonomous: true
requirements:
  - PIPE-04
  - PIPE-03

must_haves:
  truths:
    - "System can connect to a local SQLite database file"
    - "Database schema can be created automatically on startup"
    - "Extracted data records can be validated against strict types"
  artifacts:
    - path: "src/database/session.py"
      provides: "SQLite connection and session management"
      exports: ["get_session", "init_db"]
    - path: "src/database/models.py"
      provides: "SQLAlchemy ORM models"
      contains: "class Base"
    - path: "src/schemas/record.py"
      provides: "Pydantic validation schemas"
  key_links:
    - from: "src/database/session.py"
      to: "src/database/models.py"
      via: "Base.metadata.create_all"
      pattern: "create_all"
---

<objective>
Establish the core data foundation by setting up SQLAlchemy ORM models, a local SQLite database connection, and Pydantic validation schemas.

Purpose: This provides the typed structures and database connection needed before any data can be extracted or saved.
Output: Working database connection, initialized tables, and Pydantic validation schemas.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Setup Database Models and Session</name>
  <files>src/database/models.py, src/database/session.py, tests/test_database.py</files>
  <behavior>
    - Test 1: init_db creates a SQLite database file and tables without errors.
    - Test 2: get_session yields a working SQLAlchemy session.
  </behavior>
  <action>
    Implement SQLAlchemy 2.0 models and session management for a local SQLite database.
    Per D-03: Use `Base.metadata.create_all(engine)` initially instead of Alembic migrations.
    Create a generic `GameRecord` model (or similar based on the HTML data structure) with basic fields like `id`, `source_file`, `raw_html_content` or extracted fields. Since specific fields aren't defined yet, include a few generic fields (e.g., `title`, `url`, `extracted_at`, `status`) and a JSON/string payload column for flexibility if needed. Ensure the DB connection uses `sqlite:///local.db` by default.
  </action>
  <verify>
    <automated>pytest tests/test_database.py -v</automated>
  </verify>
  <done>SQLite database file is created and tables are initialized when tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Setup Pydantic Schemas</name>
  <files>src/schemas/record.py, tests/test_schemas.py</files>
  <behavior>
    - Test 1: Valid dictionary successfully parses into the schema.
    - Test 2: Invalid data types raise ValidationError.
  </behavior>
  <action>
    Create Pydantic models (v2) corresponding to the SQLAlchemy models.
    Per D-02: These schemas will be used for strict normalization. Define a `GameRecordSchema` that enforces strict typing on the fields defined in the database model. This ensures data quality before it hits the database.
  </action>
  <verify>
    <automated>pytest tests/test_schemas.py -v</automated>
  </verify>
  <done>Pydantic schemas can validate correct data and reject malformed data with standard errors.</done>
</task>

</tasks>

<verification>
Ensure tests pass, the `local.db` file can be created, and the Pydantic schemas enforce type constraints correctly.
</verification>

<success_criteria>
- `src/database/session.py` provides a working SQLAlchemy engine and sessionmaker.
- Tests confirm tables are created via `create_all()`.
- Pydantic models validate successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-data-extraction-storage/01-01-SUMMARY.md`
</output>
